FROM mysql:8.0

# cron関連のインストールを削除し、必要なユーティリティのみ残す
RUN microdnf install -y ncurses \
 && microdnf clean all

# batch フォルダのコピー
COPY batch /batch

# 実行権限の付与（念のため）
RUN chmod +x /batch/*.sh

# batchコンテナ自体は常駐させるだけにする（dkronから叩かれるため）
CMD ["tail", "-f", "/dev/null"]